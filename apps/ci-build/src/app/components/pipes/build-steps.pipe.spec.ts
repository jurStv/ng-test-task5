import { BuildStepsPipe } from './build-steps.pipe';

describe('BuildStepsPipe', () => {
  it('create an instance', () => {
    const pipe = new BuildStepsPipe();
    expect(pipe).toBeTruthy();
  });
});
