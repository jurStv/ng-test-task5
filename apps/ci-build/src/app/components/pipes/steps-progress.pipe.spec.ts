import { StepsProgressPipe } from './steps-progress.pipe';

describe('StepsProgressPipe', () => {
  it('create an instance', () => {
    const pipe = new StepsProgressPipe();
    expect(pipe).toBeTruthy();
  });
});
