import { InitialStepPipe } from './initial-step.pipe';

describe('InitialStepPipe', () => {
  it('create an instance', () => {
    const pipe = new InitialStepPipe();
    expect(pipe).toBeTruthy();
  });
});
